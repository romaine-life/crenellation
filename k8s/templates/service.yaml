{{- if eq (include "crenellation.renderHot" .) "true" }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "crenellation.resourceName" . }}
  namespace: {{ include "crenellation.namespace" . }}
  labels:
    app: {{ include "crenellation.resourceName" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "crenellation.resourceName" . }}
  ports:
    - name: http
      port: 80
      # Served port: the live-preview edge when livePreview is enabled (it fronts
      # the backend), the backend's own "http" port otherwise. The include is
      # guarded because the helper lives in the vendored partial (absent on
      # non-preview renders); the disabled branch keeps normal / validation
      # renders targeting "http" unchanged.
      targetPort: {{ if .Values.livePreview.enabled }}{{ include "live-preview-edge.servedPortName" (dict "spec" .Values.livePreview "appPortName" (include "crenellation.appPortName" .)) }}{{ else }}http{{ end }}
      protocol: TCP
{{- end }}
